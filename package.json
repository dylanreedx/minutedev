{
  "name": "minutedev",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:push": "pnpm --filter @minute/db db:push",
    "db:studio": "pnpm --filter @minute/db db:studio",
    "db:generate": "pnpm --filter @minute/db db:generate"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "packageManager": "pnpm@10.2.0"
}
